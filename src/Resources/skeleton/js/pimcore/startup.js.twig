pimcore.registerNS("pimcore.plugin.{{ bundle }}");

pimcore.plugin.{{ bundle }} = Class.create({

    initialize: function () {
        document.addEventListener(pimcore.events.pimcoreReady, this.pimcoreReady());
        pimcore.plugin.broker.registerPlugin(this);
    },

    pimcoreReady: function (e) {
        // alert("{{ bundle }} ready!");
    }
});

var {{ bundle }}Plugin = new pimcore.plugin.{{ bundle }}();
